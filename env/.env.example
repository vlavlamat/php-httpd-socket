# ===========================================
# PHP-Httpd-Socket Environment Configuration
# ===========================================

# MySQL Database Configuration
MYSQL_ROOT_PASSWORD=secret-root-password
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=app

# phpMyAdmin Configuration
PMA_HOST=mysql-httpd-socket
PMA_ARBITRARY=1

# Xdebug (optional; enabled via docker-compose.xdebug.yml by default when using make xdebug-up)
# Set these only if you want to enable Xdebug without the overlay compose file
XDEBUG_MODE=
XDEBUG_START=
